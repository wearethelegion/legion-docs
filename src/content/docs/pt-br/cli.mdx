---
title: CLI
description: Opções e comandos da CLI do legion.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do legion, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
legion
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o legion programaticamente.

```bash
legion run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do legion.

```bash
legion [project]
```

#### Opções

| Flag         | Curto | Descrição                                                                  |
| ------------ | ----- | -------------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue a última sessão                                                   |
| `--session`  | `-s`  | ID da sessão para continuar                                                |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| `--prompt`   |       | Prompt a ser usado                                                         |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| `--agent`    |       | Agente a ser usado                                                         |
| `--port`     |       | Porta para escutar                                                         |
| `--hostname` |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do legion também possui os seguintes comandos.

---

### agent

Gerencie agentes para o legion.

```bash
legion agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do legion já em execução, iniciado via comandos `serve` ou `web`.

```bash
legion attach [url]
```

Isso permite usar o TUI com um backend legion remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
legion web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
legion attach http://10.20.30.40:4096
```

#### Opções

| Flag        | Curto | Descrição                                |
| ----------- | ----- | ---------------------------------------- |
| `--dir`     |       | Diretório de trabalho para iniciar o TUI |
| `--session` | `-s`  | ID da sessão para continuar              |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
legion agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
legion agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
legion auth [command]
```

---

#### login

O legion é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `legion auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/legion/auth.json`.

```bash
legion auth login
```

Quando o legion é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
legion auth list
```

Ou a versão curta.

```bash
legion auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
legion auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
legion github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
legion github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
legion github run
```

##### Opções

| Flag      | Descrição                                        |
| --------- | ------------------------------------------------ |
| `--event` | Evento simulado do GitHub para executar o agente |
| `--token` | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
legion mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
legion mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
legion mcp list
```

Ou use a versão curta.

```bash
legion mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
legion mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
legion mcp auth list
```

Ou use a versão curta.

```bash
legion mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
legion mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
legion mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
legion models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
legion models anthropic
```

#### Opções

| Flag        | Descrição                                                             |
| ----------- | --------------------------------------------------------------------- |
| `--refresh` | Atualiza o cache de modelos a partir do models.dev                    |
| `--verbose` | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no legion.

```bash
legion models --refresh
```

---

### run

Execute o legion em modo não interativo passando um prompt diretamente.

```bash
legion run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "legion run"
legion run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `legion serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
legion serve

# Em outro terminal, execute comandos que se anexam a ele
legion run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag         | Curto | Descrição                                                                     |
| ------------ | ----- | ----------------------------------------------------------------------------- |
| `--command`  |       | O comando a ser executado, use mensagem para argumentos                       |
| `--continue` | `-c`  | Continue a última sessão                                                      |
| `--session`  | `-s`  | ID da sessão para continuar                                                   |
| `--fork`     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)    |
| `--share`    |       | Compartilhe a sessão                                                          |
| `--model`    | `-m`  | Modelo a ser usado na forma de provider/model                                 |
| `--agent`    |       | Agente a ser usado                                                            |
| `--file`     | `-f`  | Arquivo(s) a serem anexados à mensagem                                        |
| `--format`   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                     |
| `--title`    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)    |
| `--attach`   |       | Anexe a um servidor legion em execução (por exemplo, http://localhost:4096) |
| `--port`     |       | Porta para o servidor local (padrão para porta aleatória)                     |

---

### serve

Inicie um servidor legion sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
legion serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do legion sem a interface TUI. Defina `LEGION_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `legion`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do legion.

```bash
legion session [command]
```

---

#### list

Liste todas as sessões do legion.

```bash
legion session list
```

##### Opções

| Flag          | Curto | Descrição                                 |
| ------------- | ----- | ----------------------------------------- |
| `--max-count` | `-n`  | Limitar às N sessões mais recentes        |
| `--format`    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do legion.

```bash
legion stats
```

#### Opções

| Flag        | Descrição                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------- |
| `--days`    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| `--tools`   | Número de ferramentas a serem mostradas (todas)                                                      |
| `--models`  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| `--project` | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
legion export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do legion.

```bash
legion import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do legion.

```bash
legion import session.json
legion import https://opncd.ai/s/abc123
```

---

### web

Inicie um servidor legion sem cabeça com uma interface web.

```bash
legion web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o legion através de uma interface web. Defina `LEGION_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `legion`).

#### Opções

| Flag         | Descrição                                             |
| ------------ | ----------------------------------------------------- |
| `--port`     | Porta para escutar                                    |
| `--hostname` | Nome do host para escutar                             |
| `--mdns`     | Habilitar descoberta mDNS                             |
| `--cors`     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
legion acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag         | Descrição                 |
| ------------ | ------------------------- |
| `--cwd`      | Diretório de trabalho     |
| `--port`     | Porta para escutar        |
| `--hostname` | Nome do host para escutar |

---

### uninstall

Desinstale o legion e remova todos os arquivos relacionados.

```bash
legion uninstall
```

#### Opções

| Flag            | Curto | Descrição                                |
| --------------- | ----- | ---------------------------------------- |
| `--keep-config` | `-c`  | Manter arquivos de configuração          |
| `--keep-data`   | `-d`  | Manter dados de sessão e snapshots       |
| `--dry-run`     |       | Mostrar o que seria removido sem remover |
| `--force`       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o legion para a versão mais recente ou uma versão específica.

```bash
legion upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
legion upgrade
```

Para atualizar para uma versão específica.

```bash
legion upgrade v0.1.48
```

#### Opções

| Flag       | Curto | Descrição                                                        |
| ---------- | ----- | ---------------------------------------------------------------- |
| `--method` | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do legion aceita as seguintes flags globais.

| Flag           | Curto | Descrição                               |
| -------------- | ----- | --------------------------------------- |
| `--help`       | `-h`  | Exibir ajuda                            |
| `--version`    | `-v`  | Imprimir número da versão               |
| `--print-logs` |       | Imprimir logs no stderr                 |
| `--log-level`  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O legion pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `LEGION_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `LEGION_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `LEGION_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `LEGION_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `LEGION_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `LEGION_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `LEGION_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `LEGION_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `LEGION_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `LEGION_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `LEGION_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `LEGION_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `LEGION_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `LEGION_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `LEGION_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `LEGION_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `LEGION_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `LEGION_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `LEGION_DISABLE_FILETIME_CHECK`     | boolean | Desabilitar verificação de tempo de arquivo para otimização           |
| `LEGION_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `LEGION_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `LEGION_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `LEGION_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `legion`) |
| `LEGION_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `LEGION_EXPERIMENTAL`                         | boolean | Habilitar todos os recursos experimentais                 |
| `LEGION_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `LEGION_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `LEGION_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `LEGION_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `LEGION_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `LEGION_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `LEGION_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `LEGION_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `LEGION_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `LEGION_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar verificação de tipo LSP experimental            |
| `LEGION_EXPERIMENTAL_MARKDOWN`                | boolean | Habilitar recursos experimentais de markdown              |
| `LEGION_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
