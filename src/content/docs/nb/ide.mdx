---
title: IDE
description: Legion-utvidelsen for VS Code, Cursor og andre IDE-er
---

Legion integreres med VS Code, Cursor eller en hvilken som helst IDE som støtter en terminal. Bare kjør `legion` i terminalen for å komme i gang.

---

## Bruk

- **Hurtigstart**: Bruk `Cmd+Esc` (Mac) eller `Ctrl+Esc` (Windows/Linux) for å åpne Legion i en delt terminalvisning, eller fokuser en eksisterende terminaløkt hvis en allerede kjører.
- **Ny økt**: Bruk `Cmd+Shift+Esc` (Mac) eller `Ctrl+Shift+Esc` (Windows/Linux) for å starte en ny Legion-terminaløkt, selv om en allerede er åpen. Du kan også klikke på Legion-knappen i UI.
- **Kontekstbevissthet**: Del automatisk ditt nåværende valg eller fane med Legion.
- **Snarveier for filreferanser**: Bruk `Cmd+Option+K` (Mac) eller `Alt+Ctrl+K` (Linux/Windows) for å sette inn filreferanser. For eksempel `@File#L37-42`.

---

## Installasjon

For å installere Legion på VS Code og populære varianter som Cursor, Windsurf, VSCodium:

1. Åpne VS Code
2. Åpne den integrerte terminalen
3. Kjør `legion` - utvidelsen installeres automatisk

Hvis du derimot vil bruke din egen IDE når du kjører `/editor` eller `/export` fra TUI, må du angi `export EDITOR="code --wait"`. [Finn ut mer](/docs/tui/#editor-setup).

---

### Manuell installasjon

Søk etter **Legion** i Extension Marketplace og klikk på **Install**.

---

### Feilsøking

Hvis utvidelsen ikke kan installeres automatisk:

- Sørg for at du kjører `legion` i den integrerte terminalen.
- Bekreft at CLI for IDE er installert:
  - For VS Code: `code` kommando
  - For Cursor: `cursor` kommando
  - For Windsurf: `windsurf` kommando
  - For VSCodium: `codium` kommando
  - Hvis ikke, kjør `Cmd+Shift+P` (Mac) eller `Ctrl+Shift+P` (Windows/Linux) og søk etter "Shell Command: Install 'code' command in PATH" (eller tilsvarende for din IDE)
- Sørg for at VS Code har tillatelse til å installere utvidelser
